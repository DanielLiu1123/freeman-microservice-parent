spring:
  data:
    mongodb:
      uri: mongodb://${embedded.mongodb.host}:${embedded.mongodb.port}/freeman_order
      dynamic:
        enable: true
        datasources:
          ds1:
            uri: mongodb://${embedded.mongodb.host}:${embedded.mongodb.port}/freeman_user
